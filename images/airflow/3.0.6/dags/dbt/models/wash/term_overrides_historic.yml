version: 2

models:
  - name: term_overrides_historic
    description: "Tracks historic customer cycle and order sizes overrides."
    columns: 
      - name: customer_id
        data_tests:
          - not_null
      - name: term_id
        data_tests:
          - not_null