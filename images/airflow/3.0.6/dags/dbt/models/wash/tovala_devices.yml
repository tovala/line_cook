version: 2

models:
  - name: tovala_devices
    description: "Current oven information for registered ovens - used when we don't have historic records."
    columns:
      - name: id
        data_tests:
          - primary_key
      - name: device_id
        data_tests:
          - primary_key