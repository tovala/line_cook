version: 2

models:
  - name: google_ad_to_channel_mapping
    description: "Google Ads fall into multiple channels, as delineated by the campaign labels. This creates the map used downstream."
    columns:
      - name: id
        data_tests:
          - primary_key
      - name: channel_name
        data_tests:
          - not_null