version: 2

models:
  - name: app_analytics_keys
    description: "Current and deprecated Fullstory keys being used by the app team."
    columns:
      - name: key_id
        data_tests:
          - primary_key
      - name: key
        data_tests:
          - primary_key