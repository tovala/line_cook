version: 2

models:
  - name: fullstory_session_to_customer
    description: "Creates a unique association between session_id and customer_id."
    columns: 
      - name: session_id
        data_tests:
          - primary_key
      - name: session_customer_id
        data_tests:
          - primary_key