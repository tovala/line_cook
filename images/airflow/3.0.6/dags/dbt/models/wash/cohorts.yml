version: 2

models:
  - name: cohorts
    description: "Table that calculates cohort and week number with and without holidays"
    columns:
      - name: term_id
        description: "Primary Key"
        data_tests:
          - primary_key