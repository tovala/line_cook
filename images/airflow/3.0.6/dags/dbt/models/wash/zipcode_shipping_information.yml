version: 2

models:
  - name: zipcode_shipping_information
    description: "Info about zip codes for delivery"
    columns:
      - name: zip_cd
        description: 'Primary key (zipcode)'
        data_tests:
          - not_null
