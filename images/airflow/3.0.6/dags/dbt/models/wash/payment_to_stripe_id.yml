version: 2

models:
  - name: payment_to_stripe_id
    description: "Maps a payment id to any associated stripe charge ids for revenue reconciliation."
    columns:
      - name: payment_id
        data_tests:
          - primary_key
          