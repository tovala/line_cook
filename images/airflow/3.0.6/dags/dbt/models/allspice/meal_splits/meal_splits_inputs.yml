version: 2

models:
  - name: meal_splits_inputs
    description: >
      Inputs for the meal splits prediction model; splits are expected to be null for future terms
    data_tests:
      - unique_columns:
          arguments:
            unique_column_list:
              - meal_sku_id
              - term_id
              - cycle
              - facility_network
