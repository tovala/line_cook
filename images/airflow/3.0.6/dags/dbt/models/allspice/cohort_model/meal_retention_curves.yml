version: 2

models:
  - name: meal_retention_curves
    description: >
      Customer meal retention decay curves
    columns:
      - name: adjusted_order_week
        data_tests:
          - primary_key
