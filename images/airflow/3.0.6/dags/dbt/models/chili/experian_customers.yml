version: 2

models:
  - name: experian_customers
    data_tests:
      # Ensure that the headers aren't changing
      - unique_columns:
          arguments:
            unique_column_list:
              - object_keys(raw_data)
            count_value: DISTINCT object_keys(raw_data)
    columns:
      - name: transaction_id
        data_tests:
          - not_null
      - name: raw_data
        data_tests:
          - not_null