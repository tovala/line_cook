version: 2

models:
  - name: dry_cookable_review_chip_selections
    columns:
      - name: cookable_review_chip_id
        data_tests:
          - not_null
      - name: cookable_review_id
        data_tests:
          - not_null