version: 2

models:
  - name: dry_experian_customers
    # If this is failing, check the upstream table for NULL customer_ids as those indicate errors
    data_tests:
      - recency:
          arguments:
            field: updated
            datepart: day
            interval: 2 
    columns:
      - name: customer_id
        data_tests:
          - primary_key