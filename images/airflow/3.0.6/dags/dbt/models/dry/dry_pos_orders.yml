version: 2

models:
  - name: dry_pos_orders
    description: "This table pulls in order details (including customer and address fields) from the new Order POS system."
    columns:
      - name: id
        data_tests:
          - primary_key
      - name: created 
        data_tests:
          - not_null