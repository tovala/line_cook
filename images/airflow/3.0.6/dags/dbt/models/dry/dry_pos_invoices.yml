version: 2

models:
  - name: dry_pos_invoices 
    description: "This table maps order IDs to their corresponding invoice IDs within the new Order POS system."
    columns:
      - name: id
        data_tests:
          - primary_key
      - name: updated 
        data_tests:
          - not_null