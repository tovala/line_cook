version: 2

models:
  - name: dry_menu_products
    columns:
      - name: product_id
        data_tests:
          - primary_key
      - name: created
        data_tests:
          - not_null
      - name: deleted
        data_tests:
          # Should always be NULL
          - expression_is_true:
              arguments:
                expression: IS NULL