version: 2

models:
  - name: dry_pos_invoice_payments
    description: "This table maps each payment line (as well as its source and account type) to its corresponding invoice."
    columns:
      - name: payment_line_id
        data_tests:
          - primary_key
      - name: created 
        data_tests:
          - not_null