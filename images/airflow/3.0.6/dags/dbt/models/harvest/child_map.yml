version: 2

models:
  - name: child_map
    desription: Children for each dbt node.
    columns:
      - name: id
        data_tests:
          - primary_key
      - name: model_id
        data_tests:
          - not_null
          - expression_is_true:
              arguments:
                expression: "LIKE 'model.spice_rack.%'"
