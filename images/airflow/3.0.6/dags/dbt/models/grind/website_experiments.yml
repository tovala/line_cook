version: 2

models:
  - name: website_experiments
    description: "Table that tracks which experiment variant a user saw within a session"
    columns:
      - name: website_experiment_key
        description: "Primary Key"
        data_tests:
          - primary_key
