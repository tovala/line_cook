version: 2

models:
  - name: cs_comments
    description: Customer Support Comments. One ticket may have many comments.
    data_tests:
      # Ensures comments are numbered uniquely
      - unique_columns:
          arguments:
            unique_column_list:
              - ticket_id
              - comment_number
