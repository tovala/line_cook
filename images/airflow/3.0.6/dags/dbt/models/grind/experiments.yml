version: 2

models:
  - name: experiments
    description: "The experiment names and it's owner in experiment tracker data."
    columns:
      - name: experiment_id
        data_tests:
          - primary_key
      - name: partition_method
        data_tests:
          - accepted_values:
              arguments:
                values: ['ModuloUserID', 'UserAspectMatching']