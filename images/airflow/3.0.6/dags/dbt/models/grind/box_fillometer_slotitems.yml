version: 2

models:
  - name: box_fillometer_slotitems
    description: "Individual slot and item information for Box Fillometer logs"
    columns:
      - name: order_id
        data_tests:
          - not_null
      - name: box_id
        data_tests:
          - not_null
      - name: slotitem_id
        data_tests:
          - primary_key