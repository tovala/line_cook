version: 2

models:
  - name: add_on_reviews
    description: "Reviews of meal add-ons, pulled from airtable"
    columns:
      - name: add_on_review_id
        description: "Primary Key"
        data_tests:
          - primary_key
