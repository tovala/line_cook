version: 2

models:
  - name: stripe_fingerprint_history
    description: "Point in time historical data about Stripe fingerprint status changes."
    columns:
      - name: status_id
        data_tests:
          - primary_key
      - name: stripe_fingerprint
        data_tests:
          - not_null