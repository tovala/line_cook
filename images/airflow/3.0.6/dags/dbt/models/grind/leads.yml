version: 2

models:
  - name: leads
    description: "Table of Leads (all users who've come to our site)"
    columns:
      - name: lead_id
        description: "Primary Key"
        data_tests:
          - primary_key
      - name: registration_time
        description: "Time (in UTC) at which customer registered"
        data_tests:
          - not_null