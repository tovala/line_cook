version: 2

models:
  - name: oven_events
    columns:
      - name: oven_event_id
        data_tests:
          - primary_key:
              arguments:
                condition: "updated >= DATEADD(day, -14, {{ current_timestamp_utc() }})"
      - name: event_source
        data_tests:
          - not_null