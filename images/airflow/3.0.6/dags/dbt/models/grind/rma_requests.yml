version: 2

models:
  - name: rma_requests
    description: "Full replica of RMA request tracking in Airtable."
    columns:
      - name: rma_airtable_id
        data_tests:
          - primary_key
      - name: rma_request_id
        data_tests:
          - primary_key