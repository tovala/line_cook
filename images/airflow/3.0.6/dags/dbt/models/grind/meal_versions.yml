version: 2

models:
  - name: meal_versions
    description: "List of meals and versions from Misevala."
    columns:
      - name: version_id
        description: "Primary Key"
        data_tests:
          - primary_key
      - name: internal_meal_name
        data_tests: 
          - not_null