version: 2

models:
  - name: payments
    description: "Payment events for all non-test users."
    columns:
      - name: payment_id
        description: "Primary key directly from raw table."
        data_tests:
          - primary_key