version: 2

models:
  - name: addresses
    description: "Table of addresses"
    columns:
      - name: address_id
        data_tests:
          - primary_key
      - name: customer_id
        data_tests:
          - primary_key
      - name: zip_cd
        description: "Zip code of the address"
        data_tests:
          - not_null