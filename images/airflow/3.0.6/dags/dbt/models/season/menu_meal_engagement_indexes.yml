version: 2

models:
  - name: menu_meal_engagement_indexes
    description: "Normalized raw engagement rate by menu size and adjust for customized meals. e.g. 20 meals menu avg engagement rate is 5%. Engagement index for a meal with 5% raw engagement rate 1."
    columns:
      - name: menu_meal_id
        description: "Primary Key"
        data_tests:
          - primary_key