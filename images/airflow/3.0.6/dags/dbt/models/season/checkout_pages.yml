version: 2

models:
  - name: checkout_pages
    description: "Table for checkout pages only"
    columns:
      - name: page_visit_id
        description: "Primary Key"
        data_tests:
          - primary_key
