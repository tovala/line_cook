version: 2

models:
  - name: meal_order_costs
    description: “Table contain 100% of costs( Ingredient, shipping, labor, disposables, cc fee(currently only has this, populate the rest as going), linehaul, and packaging), aggregated at an order level."
    columns:
      - name: meal_order_id 
        description: "Primary Key"
        data_tests:
          - primary_key
          