version: 2

models:
  - name: oven_error_messages
    description: "Includes all error code events - Gen2 only."
    columns:
      - name: error_group
        data_tests:
          - accepted_values:
              arguments:
                values: ['2', '3', '5', '8']
      # Verifies that all codes have been appropriately categorized
      - name: is_valid_code
        data_tests:
          - expression_is_true:
              arguments:
                expression: ""