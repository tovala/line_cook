version: 2

models:
  - name: commitment_customers
    columns:
      - name: customer_id
        data_tests:
          - primary_key
      - name: commitment_status
        data_tests:
          - not_null