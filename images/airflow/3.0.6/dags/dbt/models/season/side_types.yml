version: 2

models:
  - name: side_types
    description: "Side types information for each internal meals"
    columns:
      - name: meal_id
        data_tests:
          - not_null
      - name: internal_meal_name
        data_tests:
          - not_null
