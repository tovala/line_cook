version: 2

models:
  - name: refunds
    description: "Payment line items split into refunds only."
    columns:
      - name: refund_id
        description: "Primary Key"
        data_tests:
          - primary_key
      - name: payment_id
        description: 'Payment ID that was refunded'
        data_tests:
          - not_null