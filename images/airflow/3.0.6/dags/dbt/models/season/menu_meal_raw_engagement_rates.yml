version: 2

models:
  - name: menu_meal_raw_engagement_rates
    description: "4 levels(subterm, cycle, facility, term) of raw engagement rates of a meal(fulfilled, non-autoselected)"
    columns:
      - name: menu_meal_id
        description: "Primary Key"
        data_tests:
          - primary_key
